<div class="modal_lb">
    <img src='{{images.3.url}}' alt='{{name}}' class='img-card_image_small' />
    <div class='modal_wrap'>
        <div class='modal_img'>
            <img src='{{images.7.url}}' alt='{{name}}' class='img-card_image' />
        </div>
        <div class="modal_content">
            <div class='modal-container'>
                <h2 class='modal-title'>INFO</h2>
                {{#if name}}
                <p class='modal_text info'>{{name}}</p>
                {{else}}
                <p class='modal_text'>------------</p>
                {{/if}}
            </div>
            <div class='modal-container'>
                <h2 class='modal-title'>WHEN</h2>
                <p class='modal_text'>{{dates.start.localDate}}</p>
                <p class='modal_text'>{{dates.start.localTime}} ({{dates.timezone}})</p>
            </div>
            <div class='modal-container'>
                <h2 class='modal-title'>WHERE</h2>
                <p class='modal_text'>{{dates.timezone}}</p>
                <p class='modal_text'>{{_embedded.venues.0.address.line1}}</p>
            </div>
            <div class='modal-container'>
                <h2 class='modal-title'>WHO</h2>
                <p class='modal_text'>{{#if _embedded.attractions}}
                {{#with _embedded.attractions.[0]}}{{name}}{{/with}}
                {{else}}
                <p class='modal_text'>------------</p>
                {{/if}}
                </p>
            </div>
            <div class='modal-container btn-container'>
                <h2 class='modal-title'>PRICES</h2>
                <p class='modal_text wrap_price'>
                    <span class='material-icons modal_icon'>qr_code</span>
                    Standart {{priceRanges.0.min}}-{{priceRanges.0.max}} {{priceRanges.0.currency}}
                </p>
                <form class="modal_link_btn" action="{{url}}" method="GET" target="_blank" rel="noopener noreferrer">
                    <button type='submit' class='modal_buy_btn'>BUY TICKETS</button>
                </form>
                <p class='modal_text wrap_price'>
                    <span class='material-icons modal_icon'>qr_code</span>
                    VIP {{priceRanges.0.min}}-{{priceRanges.0.max}} {{priceRanges.0.currency}}
                </p>
                <form class="modal_link_btn" action="{{url}}" method="GET" target="_blank" rel="noopener noreferrer">
                    <button type='submit' class='modal_buy_btn'>BUY TICKETS</button>
                </form>
            </div>
        </div>
    </div>
    <div class="modal_more_button__wrap">
      <a href='https://en.wikipedia.org/wiki/{{name}}' class='link modal_more_button' target='_blank'
        rel='noopener noreferrer' rel='noopener noreferrer'>MORE FROM THIS AUTHOR</a>
    </div>


</div>

